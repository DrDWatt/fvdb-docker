# =============================================================================
# ISAAC ROSBAG WORKFLOW - Docker Compose
# =============================================================================
# SVO to ROSBAG conversion workflow for NVIDIA ISAAC Sim and ISAAC Lab
#
# Usage:
#   docker compose -f docker-compose.isaac-rosbag.yml up -d
#
# Service Ports:
#   8009 - SVO to ROSBAG Converter (Main Workflow Entry)
#   8010 - ISAAC Sim
#   8011 - ISAAC Lab
#   8012 - ISAAC Viewer (SVO to Gaussian Splat workflow)
#   8014 - cuSFM Service (GPU-accelerated SfM with TensorRT)
#
# Swagger API Documentation:
#   http://localhost:8009/api - SVO Converter API
#   http://localhost:8010/api - ISAAC Sim API
#   http://localhost:8011/api - ISAAC Lab API
#   http://localhost:8012/api - ISAAC Viewer API
# =============================================================================

services:
  # ===========================================================================
  # SVO TO ROSBAG CONVERTER
  # Converts ZED X stereo camera SVO files to ROSBAG format
  # Swagger: http://localhost:8009/api
  # ===========================================================================
  svo-converter:
    build:
      context: ./isaac-rosbag-service
      dockerfile: Dockerfile
    image: svo-rosbag-converter:latest
    container_name: svo-converter
    ports:
      - "8009:8009"
    volumes:
      - ./isaac-data/svo-uploads:/app/uploads
      - ./isaac-data/rosbags:/app/outputs
      - ./isaac-data/processing:/app/processing
      - ./isaac-rosbag-service/svo_converter_service.py:/app/svo_converter_service.py:ro
    environment:
      - UPLOAD_DIR=/app/uploads
      - OUTPUT_DIR=/app/outputs
      - PROCESSING_DIR=/app/processing
      - PORT=8009
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8009/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - isaac-net

  # ===========================================================================
  # ISAAC SIM SERVICE
  # Robotics simulation platform built on NVIDIA Omniverse
  # Swagger: http://localhost:8010/api
  # ===========================================================================
  isaac-sim:
    build:
      context: ./isaac-sim-service
      dockerfile: Dockerfile
    image: isaac-sim-service:latest
    container_name: isaac-sim
    ports:
      - "8010:8010"
    volumes:
      - ./isaac-data/rosbags:/app/rosbags:ro
      - ./isaac-data/scenes:/app/scenes
      - ./isaac-data/sim-outputs:/app/outputs
      - ./isaac-sim-service/isaac_sim_service.py:/app/isaac_sim_service.py:ro
    environment:
      - ROSBAG_DIR=/app/rosbags
      - SCENE_DIR=/app/scenes
      - OUTPUT_DIR=/app/outputs
      - PORT=8010
    restart: unless-stopped
    depends_on:
      - svo-converter
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8010/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - isaac-net

  # ===========================================================================
  # ISAAC LAB SERVICE
  # Reinforcement learning framework for robotics
  # Swagger: http://localhost:8011/api
  # ===========================================================================
  isaac-lab:
    build:
      context: ./isaac-lab-service
      dockerfile: Dockerfile
    image: isaac-lab-service:latest
    container_name: isaac-lab
    ports:
      - "8011:8011"
    volumes:
      - ./isaac-data/checkpoints:/app/checkpoints
      - ./isaac-data/logs:/app/logs
      - ./isaac-data/lab-outputs:/app/outputs
      - ./isaac-lab-service/isaac_lab_service.py:/app/isaac_lab_service.py:ro
    environment:
      - CHECKPOINT_DIR=/app/checkpoints
      - LOG_DIR=/app/logs
      - OUTPUT_DIR=/app/outputs
      - PORT=8011
    restart: unless-stopped
    depends_on:
      - isaac-sim
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8011/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - isaac-net

  # ===========================================================================
  # ISAAC VIEWER
  # SVO to Gaussian Splat workflow with cuSFM (GPU-accelerated SfM)
  # Swagger: http://localhost:8012/api
  # ===========================================================================
  isaac-viewer:
    build:
      context: ./isaac-viewer
      dockerfile: Dockerfile
    image: isaac-viewer:latest
    container_name: isaac-viewer
    ports:
      - "8012:8012"
    volumes:
      - ./isaac-data/svo-uploads:/app/svo
      - ./isaac-data/rosbags:/app/rosbags
      - ./isaac-data/viewer-outputs:/app/outputs
      - ./isaac-data/frames:/app/frames
      - ./isaac-viewer/isaac_viewer_service.py:/app/isaac_viewer_service.py:ro
    environment:
      - SVO_DIR=/app/svo
      - ROSBAG_DIR=/app/rosbags
      - OUTPUT_DIR=/app/outputs
      - FRAME_DIR=/app/frames
      - PORT=8012
      - COLMAP_SERVICE_URL=http://host.docker.internal:8003
      - CUSFM_SERVICE_URL=http://cusfm-service:8014
      - TRAINING_SERVICE_URL=http://host.docker.internal:8000
      - FVDB_VIEWER_URL=http://host.docker.internal:8085
      - DEPTH_SERVICE_URL=http://host.docker.internal:8013
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    depends_on:
      - svo-converter
      - cusfm-service
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8012/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - isaac-net
      - workflow-net

  # ===========================================================================
  # cuSFM SERVICE
  # GPU-accelerated Structure from Motion using NVIDIA cuSFM with TensorRT
  # Swagger: http://localhost:8014/api
  # ===========================================================================
  cusfm-service:
    build:
      context: ./cusfm-service
      dockerfile: Dockerfile
    container_name: cusfm-service
    runtime: nvidia
    ports:
      - "8014:8014"
    volumes:
      - ./isaac-data/frames:/app/frames
      - ./cusfm-data/processing:/app/processing
      - ./cusfm-data/outputs:/app/outputs
      - ./cusfm-data/temp:/app/temp
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - PYCUSFM_DIR=/app/pycusfm/pycusfm
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8014/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - isaac-net
      - workflow-net

# =============================================================================
# NETWORKS
# =============================================================================
networks:
  isaac-net:
    driver: bridge
    name: isaac-rosbag-net
  workflow-net:
    external: true
    name: workflow-net

# =============================================================================
# VOLUMES
# =============================================================================
volumes:
  isaac-data:
